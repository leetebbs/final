import React from "react";
import {ethers} from "ethers";

export const getRaffleCount = async () => {

    // call the server to get the raffle count

    //call the server to get the active raffles from the raffle counts

    
    
}